# Database — SQLite for local demo
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me-to-a-random-secret-at-least-32-chars"

# Google OAuth (optional — leave blank to disable Google SSO)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Email (Ethereal for local dev — auto-generated if left blank)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="noreply@swoopfunding.com"

# Admin seed password
ADMIN_PASSWORD="Admin!234"
